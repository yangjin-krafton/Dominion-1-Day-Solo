# Asset 폴더

이 폴더는 게임 리소스 파일을 보관합니다.

## 포함되는 파일:
- 이미지 파일 (PNG, JPG, SVG 등)
- 사운드 파일 (MP3, WAV, OGG 등)
- 데이터 파일 (JSON, CSV 등)
- 폰트 파일 (TTF, WOFF 등)

## 권장 하위 구조:
```
asset/
├── images/
├── sounds/
├── data/
└── fonts/
```
